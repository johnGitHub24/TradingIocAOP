# TradingIocAOP — 開發專案規格書

> **本文件為 TradingIocAOP 的唯一主規格書（開發權威）。**
> 主題：以「交易下單／風控」情境示範 **依賴注入（DI）、控制反轉（IoC）、AOP、模組化架構**。
> 技術棧：Java 21 · Spring Boot 3.2.2 · Gradle 多模組 · H2/JPA · Spring AOP · JUnit 5 · Mockito · AssertJ。
> 開發、測試、面試準備均以本文件為準。

---

## 第 0 章　專案定位與文件體系

### 0.1 專案定位

| 項目 | 內容 |
|------|------|
| 名稱 | TradingIocAOP |
| 目的 | 用最小可運作的交易情境，把「DI／IoC／AOP／模組化」四個概念徹底攤開示範 |
| 定位 | Trading 入門系列的「框架原理」補充專案 |
| 學習目的 | 同時看懂「手刻的 IoC/AOP 原理」與「Spring 工業級實作」，理解輪子怎麼造、以及框架幫你做了什麼 |
| 產出 | 一個 3 模組 Gradle 專案 + 完整單元／整合測試 + 文件體系 |

### 0.2 混合式教學設計（本專案特色）

本專案刻意採「**先手刻、再對照**」的雙軌設計：

| 軌道 | 模組 | 說明 |
|------|------|------|
| 手刻原理 | `mini-ioc` | 自己實作約 150 行的迷你 IoC 容器 + JDK 動態代理 AOP，示範「輪子怎麼造」 |
| 工業實作 | `trading-app` | 同樣的交易情境改用 Spring 原生 IoC/DI + Spring AOP（六大切面），示範實務怎麼用 |

### 0.3 文件體系

| # | 文件 | 用途 |
|---|------|------|
| 1 | `開發專案規格書.md`（本文件） | 主規格（權威） |
| 2 | `API規格書.md` | REST API 契約 |
| 3 | `docs/功能流程說明.md` | 每個功能怎麼跑（含程式對照與流程圖） |
| 4 | `docs/初學者學習說明書.md` | Day 1~2 入門節奏 |
| 5 | `docs/架構學習導引.md` | 概念對照表 + 面試一句話 |
| 6 | `docs/測試與CI.md` | Case ID 主表 + fixture + CI + 擴充 SOP |
| 7 | `README.md` | 快速啟動入口 |

### 0.4 外部參考（採用／不採用）

| 來源 | 採用範圍 | 不採用 |
|------|----------|--------|
| TransactionClosedStateMachine | 文件模板、測試 `@Tag` 分流、Case ID 命名、fixture 機制 | 狀態機業務 |
| APIGatewayMQ | Gradle 多模組結構、單元/整合測試分流、fixture 目錄慣例 | Kafka／WebFlux／限流 |

---

## 第 1 章　系統範圍

### 1.1 核心功能

- 交易下單（風控 → 報價 → 落庫 → 通知）。
- 報價查詢（示範 AOP 快取）。
- 切面觀測報表（把六大切面的效果攤開）。
- 手刻 IoC 容器 + 手刻 AOP 的獨立示範（`mini-ioc`）。

### 1.2 不在範圍

- 真實撮合、行情連線、下單回報。
- 分散式、訊息佇列、快取中介軟體（Redis）。
- 使用者認證授權。

### 1.3 技術棧

| 層 | 技術 |
|----|------|
| 語言 | Java 21 |
| 框架 | Spring Boot 3.2.2 |
| 建置 | Gradle 多模組（wrapper 8.5） |
| 持久化 | Spring Data JPA + H2（in-memory, `MODE=PostgreSQL`） |
| AOP | spring-boot-starter-aop（AspectJ 註解 + Spring 代理） |
| 驗證 | spring-boot-starter-validation |
| API 文件 | springdoc-openapi 2.3.0（Swagger UI） |
| 測試 | JUnit 5 + Mockito + AssertJ |

### 1.4 啟動方式

```powershell
. .\scripts\env.ps1          # 設定 JAVA_HOME 為 JDK 21
.\gradlew.bat :trading-app:bootRun
# 或執行 mini-ioc 示範：
.\gradlew.bat :mini-ioc:run
```

---

## 第 2 章　模組化架構

### 2.1 模組結構

```text
TradingIocAOP/  (Gradle 多模組)
├── common/          純 POJO：DTO / enum / 領域模型（零框架相依）
│   └── com.trading.common: OrderRequest, OrderResult, Quote, Side, OrderStatus
├── mini-ioc/        手刻 IoC 容器 + 手刻 AOP（相依 common）
│   ├── annotation:  @Component, @Inject
│   ├── container:   MiniApplicationContext, ClasspathScanner, BeanException
│   ├── aop:         MethodInterceptor, Invocation, InterceptorChain,
│   │                ProxyFactory, LoggingInterceptor, TimingInterceptor
│   └── demo:        OrderPlacer/RiskChecker/PricingGateway (+impl) + MiniIocDemo
└── trading-app/     Spring Boot 應用（相依 common）
    ├── api:         OrderController, PricingController, AspectReportController
    ├── application: OrderService, RiskService, PricingService, NotificationService
    ├── aspect:      六大切面 + AspectRecorder + annotation(@Cacheable,@Retryable)
    ├── infrastructure: OrderEntity, OrderRepository
    ├── dto:         PlaceOrderRequest, ErrorResponse
    └── config:      GlobalExceptionHandler, OpenApiConfig
```

### 2.2 模組相依方向

```text
common  ←────  mini-ioc
   ↑
   └────────  trading-app
```

`common` 不相依任何人；`mini-ioc` 與 `trading-app` 各自相依 `common`，彼此不相依。這正是「模組邊界」的實踐：共用契約下沉到 `common`，實作各自獨立。

### 2.3 層職責

| 層 | 職責 | 禁止 |
|----|------|------|
| api（Controller） | 收發 HTTP、參數驗證 | 寫商業邏輯 |
| application（Service） | 商業邏輯、編排 | 碰 HTTP、算 SQL 字串 |
| aspect | 橫切關注點（記錄/計時/稽核/例外/快取/重試） | 混入業務規則 |
| infrastructure | JPA 存取 | 資料轉換、商業邏輯 |

---

## 第 3 章　控制反轉與依賴注入

### 3.1 手刻容器（mini-ioc）核心流程

1. **註冊**：`register(Class...)` 或 `scan(basePackage)` 收集 `@Component`。
2. **解析**：`getBean(type)` 遞迴解析建構子參數——相依由容器決定從哪來（控制反轉）。
3. **單例**：每型別建立一次並快取。
4. **AOP**：若登記攔截器且元件有實作介面，回傳 JDK 動態代理。

### 3.2 手刻 vs Spring 對照

| 概念 | mini-ioc（手刻） | trading-app（Spring） |
|------|------------------|------------------------|
| 元件標記 | `@Component`（自訂） | `@Component`/`@Service` |
| 建構子注入 | 反射挑選建構子 + 遞迴解析 | 自動 |
| 容器 | `MiniApplicationContext` | `ApplicationContext` |
| 取得 bean | `getBean(Class)` | `@Autowired` / 建構子 |
| 掃描 | `ClasspathScanner` | `@ComponentScan` |
| 工廠式註冊 | （未實作） | `@Configuration` + `@Bean`（見 `OpenApiConfig`） |

### 3.3 為什麼用建構子注入

- 相依「必填」且不可變（`final`），物件建立後即完整。
- 易於測試（`new OrderService(mockA, mockB, ...)`）。
- 可在編譯期發現缺漏，避免欄位注入的隱藏依賴。

---

## 第 4 章　AOP 六大切面（trading-app）

| # | 切面 | 通知類型 | 切點 | 作用 |
|---|------|----------|------|------|
| 1 | `LoggingAspect` | `@Around` | `execution(* ..application.*Service.*(..))` | 記錄方法進出與參數 |
| 2 | `PerformanceAspect` | `@Around` | 同上 | 計時，超過門檻告警 |
| 3 | `ExceptionAspect` | `@AfterThrowing` | 同上 | 統一例外攔截與告警 |
| 4 | `RetryAspect` | `@Around` | `@annotation(Retryable)` | 失敗自動重試 |
| 5 | `CacheAspect` | `@Around` | `@annotation(Cacheable)` | 方法結果快取 |
| 6 | `AuditAspect` | `@AfterReturning` | `OrderService.placeOrder(..)` | 成交稽核軌跡 |

### 4.1 切面執行順序（@Order）

```text
Logging(10) → Performance(20) → Exception(30) → Retry(40) → Cache(50) → 方法本體
（AuditAspect 為 @AfterReturning，於方法成功回傳後觸發）
```

數字越小越外層。重試包在快取外、方法本體最內，符合「重試整個含快取的呼叫、快取貼近方法」的語意。

### 4.2 觀測機制

六大切面都把觀測結果寫入單例 `AspectRecorder`（由 IoC 容器注入共用），可透過 `GET /api/v1/aspects/report` 觀察，整合測試亦以它斷言切面確實生效。

---

## 第 5 章　資料庫規格

### 5.1 資料表 `orders`

| 欄位 | 型別 | 說明 |
|------|------|------|
| order_id | VARCHAR(40) PK | 系統訂單編號 |
| client_order_id | VARCHAR(64) | 客戶端冪等鍵 |
| symbol | VARCHAR(20) | 商品代碼 |
| side | VARCHAR(8) | BUY/SELL |
| quantity | INT | 數量 |
| executed_price | DECIMAL(18,4) | 成交價（報價中間價） |
| status | VARCHAR(12) | ACCEPTED/FILLED/REJECTED |
| created_at | TIMESTAMP | 建立時間 |

對應整合測試：`TradingIntegrationTest.ORDER_INT_001`（落庫）、`RISK_INT_001`（不落庫）。

---

## 第 6 章　測試規格

### 6.1 測試分層

| 層級 | Tag | Gradle 任務 | 需 DB |
|------|-----|-------------|-------|
| 單元測試 | `@Tag("unit")` | `:module:test` | 否 |
| 整合測試 | `@Tag("integration")` | `:module:integrationTest` | H2 |
| 全部 | — | `gradlew checkAll` | 是 |

### 6.2 Case ID 對照表

| Case ID | 模組 | 類型 | 測試重點 |
|---------|------|------|----------|
| IOC_001 | mini-ioc | unit | 建構子注入解析相依圖 |
| IOC_002 | mini-ioc | unit | 單例：兩次取得同一實例 |
| IOC_003 | mini-ioc | unit | 未知型別拋 BeanException |
| IOC_004 | mini-ioc | unit | 掃描套件找到 @Component |
| AOP_001 | mini-ioc | unit | LoggingInterceptor 記錄進出 |
| AOP_002 | mini-ioc | unit | TimingInterceptor 記錄耗時 |
| AOP_003 | mini-ioc | unit | 攔截器鏈洋蔥式順序 |
| IOC_INT_001 | mini-ioc | integration | 掃描+注入+代理+下單全流程 |
| RISK_001 | trading-app | unit | 風控通過 |
| RISK_002 | trading-app | unit | 數量超限拋 R002 |
| RISK_003 | trading-app | unit | 名目金額超限拋 R003 |
| ORDER_001 | trading-app | unit | 下單成功回傳 FILLED |
| ORDER_002 | trading-app | unit | 風控拒絕傳播、不落庫 |
| ORDER_003 | trading-app | unit | 通知失敗不影響成交 |
| ORDER_API_001 | trading-app | unit | POST 下單回 201 |
| ORDER_API_002 | trading-app | unit | 驗證失敗回 400 |
| ORDER_INT_001 | trading-app | integration | 下單成功 + Logging/Timing/Audit 生效 |
| RISK_INT_001 | trading-app | integration | 超限回 422 + Exception 切面記錄 |
| VALIDATION_INT_001 | trading-app | integration | 缺欄位回 400 |
| CACHE_INT_001 | trading-app | integration | 重複報價命中 Cache 切面 |
| RETRY_INT_001 | trading-app | integration | 通知重試後成交仍成功 |

### 6.3 Fixture

- 存放：`docs/test-data/placeOrder/*.json`
- 載入：`com.trading.app.support.TradingTestFixtures.loadPlaceOrder(caseId)`
- 相對路徑以 rootProject 為基準（test 任務設定 `workingDir = rootProject.projectDir`）。

---

## 第 7 章　驗收標準（DoD）

```text
□ gradlew checkAll 全綠（common / mini-ioc / trading-app）
□ mini-ioc：手刻容器可建構子注入、單例、掃描、AOP 代理
□ trading-app：POST /api/v1/orders 成功回 201 且落庫
□ 風控超限回 422 並帶 ruleCode
□ 六大切面皆可於 /api/v1/aspects/report 觀察到效果
□ 報價重複查詢命中快取（computeCount 只 +1）
□ 通知失敗經 RetryAspect 重試後成交仍成功
□ Swagger UI 可測試所有端點
□ 21 個 Case ID 全數對應到實際測試方法
```

---

## 附錄 A　與姊妹專案概念對照

| 概念 | 本專案 | TransactionClosedStateMachine | APIGatewayMQ |
|------|--------|-------------------------------|--------------|
| 主題 | DI/IoC/AOP/模組化 | 狀態機 | 高併發削峰 |
| 架構 | 3 模組 | 單模組分層 | 3 模組 |
| 測試分流 | `@Tag` | `@Tag` | `@Tag` |
| Case ID | ✅ | ✅ | ✅ |

---

*最後更新：2026-07-07 | 技術棧：Java 21 · Spring Boot 3.2.2 · Gradle 多模組 · Spring AOP · JUnit 5*

spring:
  datasource:
    url: jdbc:h2:mem:tradingdb-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
